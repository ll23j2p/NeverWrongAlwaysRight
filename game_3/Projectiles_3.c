#include "Game_3.h"

void UpdateBullets(void) {
    // Loop through all bullets and update their position/check collisions
}

void UpdateRays(void) {
    // Loop through all rays and update their animations/check collisions
}